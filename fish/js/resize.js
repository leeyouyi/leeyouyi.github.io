function resize(gameSize) {

    // if (width === undefined) {
    //     width = game.gameScene.width;
    // }
    // if (height === undefined) {
    //     height = game.gameScene.height;
    // }
    var width = gameSize.width;
    var height = gameSize.height;

    // console.log(gameSize)
    // gameSize.setSize(window.innerWidth, 500)
    // gameSize.setSize(1000, 500)
    // console.log(game.gameScene.width)
    // app.style.width = width + 'px'
    // app.style.height = height + 'px'

    // canvas.style.width = width + 'px'
    // canvas.style.height = height + 'px'



    // this.cameras.resize(width, height);

    // this.bg.setSize(width, height);
    // game.bg.setSize(width, height)
    // game.bg.setScale(.5)
    // canvas.setAttribute("width", width + 'px')
    // canvas.setAttribute("height", height + 'px')
    // let x = Math.random() * 1000
    // let y = Math.random() * 1000
    // game.canvas.width = width
    // game.canvas.height = height
    // game.gameScene.cameras.resize(width, height)
    // game.gameScene.bg.setSize(width, width)
    // bottomBar.x = width / 2
    // bottomBar.y = height - 70
    // cannonMinus.x = width / 2 - 80
    // cannonMinus.y = height - 30
    // cannonPlus.x = width / 2 + 80
    // cannonPlus.y = height - 30
    // cannon1.x = width / 2
    // cannon1.y = height - 100
    // var x = width / 2 - 60
    // for (let i = 0; i < 6; i++) {
    //     number.ary[i].x = x
    //     number.ary[i].y = height - 45
    //     x += 20

    // }

}
// window.addEventListener('resize', function (event) {
//     resize(window.innerWidth, window.innerHeight);
// }, false);